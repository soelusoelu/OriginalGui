#pragma once

#include "../../../Math/Math.h"

namespace GuiWidgetConstant {
constexpr float FRAME_WIDTH = 256.f;
constexpr float FRAME_HEIGHT = 24.f;
const Vector2 FRAME_SIZE = Vector2(FRAME_WIDTH, FRAME_HEIGHT);

constexpr float PADDING = 2.f;
}
